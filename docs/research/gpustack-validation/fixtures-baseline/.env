# Baseline GPUStack credentials (REDACTED for the repo record; real values live on the remote).
# - GPUSTACK_BOOTSTRAP_PASSWORD : initial password for the default "admin" user (first bootstrap only).
# - GPUSTACK_TOKEN   : shared server registration secret (worker --token).
GPUSTACK_BOOTSTRAP_PASSWORD=<REDACTED>
GPUSTACK_TOKEN=<REDACTED>
