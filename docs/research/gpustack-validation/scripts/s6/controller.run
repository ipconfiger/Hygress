#!/command/with-contenv sh
# no-op for the Higress Controller that is being replaced by Hygress (gateway/run).
# LONG-SLEEP, NOT exit 0: s6-supervise would restart-loop a fast-exiting longrun.
echo "hygress: Higress 'controller' disabled (CRD consumption moved into hygress)."
exec sleep infinity
